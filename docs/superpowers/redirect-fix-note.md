# Vale Decide Admin Redirect Fix

Corrige o fluxo de magic link do painel administrativo para usar explicitamente `https://vale-decide.vercel.app/admin` como destino de produção, evitando herdar `localhost` de qualquer ambiente.
